---
- name: "BASE | Cấu hình Static Routes"
  cisco.ios.ios_config:
    lines:
      - "ip route {{ item.dest }} {{ item.mask }} {{ item.next_hop }}"
  loop: "{{ static_routes | default([]) }}"
  when: static_routes is defined
